# 多资产架构

核心层只定义因子的数据契约、可得性、版本和输出。市场差异由适配器、股票池规则、成本模型和专属评估器承担。

```text
因子卡片 / 注册表
        ↓
统一 FactorSpec 与 FactorOutput
        ↓
数据适配器 ── 因子实现 ── 评估器
        ↓
股票横截面 / 期货时序 / 加密跨交易所 / 高频微观结构
```

未来扩展不得将期货换月、加密资金费率或高频订单簿规则隐含在通用因子函数中；这些假设必须成为 `FactorSpec` 或市场专属配置的一部分。
